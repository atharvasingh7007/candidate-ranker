# Core dependencies (ranking step - must run on CPU, 16GB RAM, no network)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# Pre-computation dependencies (runs on Kaggle with GPU)
sentence-transformers>=2.2.0
xgboost>=2.0.0

# JD parsing (optional, only for pre-computation)
python-docx>=0.8.11

# Sandbox / demo
streamlit>=1.30.0
